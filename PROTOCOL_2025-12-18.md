# SAP OData Service Registration Protocol

## Date: 2025-12-18

### Summary

Attempted to register OData services for BO4E CDS views in S/4HANA. Discovered missing `@OData.publish: true` annotation and fixed it in git, but the changes were not yet pulled into SAP.

---

### Timeline

#### ~11:30 - Initial OData Service Registration Attempt

- Transaction: `/IWFND/MAINT_SERVICE`
- Attempted to register 16 CDS-based OData services
- **Successfully registered (7):**

    - /HFQ/CDS_BO_MESSLOKATION_CDS
    - /HFQ/CDS_BO_ZAEHLER_CDS
    - /HFQ/CDS_BO_EM_CDS
    - /HFQ/CDS_BO_EM_PROFILE_CDS
    - /HFQ/CDS_BO_EM_ZAEHLERSTA_CDS
    - /HFQ/CDS_BO_BENACHRICHTIGU_CDS
    - /HFQ/CDS_BO_GPARTNER_CDS

- **Failed with "No Backend Services found" (9):**
    - /HFQ/CDS_BO_VERTRAG_CDS
    - /HFQ/CDS_BO_MALO_202106_CDS
    - /HFQ/CDS_GPROLLE_CDS
    - /HFQ/CDS_KONTAKTWEG_CDS
    - /HFQ/CDS_NETZ_CDS
    - /HFQ/CDS_COM_ADRESSE_CDS
    - /HFQ/CDS_COM_NOTIZ_CDS
    - /HFQ/CDS_VERBRAUCH_ABLESU_CDS
    - /HFQ/CDS_KEY_VALUE_DUMMY_CDS_CDS

#### ~12:00 - Root Cause Investigation

- Checked SE11 for SQL views - views exist
- Compared successful CDS view (CDS_BO_MESSLOKATION) with failed one (CDS_BO_VERTRAG)
- **Root cause identified:** Missing `@OData.publish: true` annotation
- CDS_BO_MESSLOKATION has the annotation on line 5 → OData service generated
- CDS_BO_VERTRAG was missing the annotation → No OData service generated

#### ~12:10 - Attempted SE80 Edit

- Tried to edit CDS in SE80
- SAP message: "Use ABAP Development Tools (ADT) in Eclipse to change Data Definition"
- Decision: Use local git checkout instead

#### ~12:15 - Git Repository Analysis

- Repository: `C:\github\hfqbo4e`
- Branch: `feat/s4changes`
- Found that most "failed" services actually HAVE the annotation in git
- Only `/HFQ/CDS_BO_VERTRAG` was truly missing the annotation in git
- File `#hfq#cds_bo_malo_202106.ddls.asddls` was completely empty (0 bytes)

#### ~12:20 - Git Commits Created

1. **Commit `9ac2ec5`** - Add @OData.publish annotation to /HFQ/CDS_BO_VERTRAG

    - File: `src/#hfq#cds_bo_vertrag.ddls.asddls`
    - Added `@OData.publish: true` annotation

2. **Commit `873c7e5`** - Add TODO placeholder for empty /HFQ/CDS_BO_MALO_202106 CDS view
    - File: `src/#hfq#cds_bo_malo_202106.ddls.asddls`
    - Added TODO comment noting the file was empty

#### ~12:25 - User pushed changes to remote

#### ~12:26 - User pulled changes via abapGit

- abapGit showed commit `873c7e5` on branch `feat/s4changes`

#### ~12:28 - CDS Activation in SE80

- Opened `/HFQ/CDS_BO_VERTRAG` in SE80
- Clicked "Aktivieren" button
- Warning dialog appeared: "Bei der Aktivierung sind Warnungen aufgetreten"
- CDS view activated successfully
- Status message: "Datendefinition /HFQ/CDS_BO_VERTRAG wurde aktiviert"
- Changed timestamp updated to: 18.12.2025 12:28:37

#### ~12:30 - Second Service Registration Attempt

- Returned to `/IWFND/MAINT_SERVICE`
- Searched for `/HFQ/CDS_BO_VERTRAG` and `/HFQ/CDS_BO_VERTRAG_CDS`
- **Still "Keine Backendservices gefunden"**

#### ~12:35 - Source Code Verification

- Opened CDS view in SE80 → "Inhalt" tab
- **DISCOVERED:** The `@OData.publish: true` annotation is NOT present in SAP!
- Source shows only:
    ```
    @AbapCatalog.sqlViewName: '/HFQ/V_VERTRAG'
    @AbapCatalog.compiler.compareFilter: true
    @AbapCatalog.preserveKey: true
    @AccessControl.authorizationCheck: #CHECK
    @EndUserText.label: 'BO4E Vertrag'
    ```
- The annotation from git commit was NOT pulled into SAP

---

### Current Status

- Git changes are committed and pushed
- abapGit shows correct commit hash `873c7e5`
- BUT: The CDS source code in SAP does NOT have the `@OData.publish: true` annotation
- The abapGit pull may not have been completed properly, or the changes need to be staged/activated differently

### ~12:45 - abapGit Pull Completed

- Navigated to ZABAPGIT transaction
- Clicked "Pull" on HFQ BO4E repository
- Multiple "Abweichende Original- und Anmeldesprache" dialogs appeared
- Clicked "In Orig.spr. pflegen" for each dialog
- Pull completed successfully: "Serialize: 176 objects, 1.40 seconds"
- Repository now shows commit **873c7e5** on branch **feat/s4changes**

### ~12:50 - Verification After Pull

- Navigated to SE80 to verify CDS source code
- Opened /HFQ/CDS_BO_VERTRAG → "Inhalt" tab
- **FINDING:** The `@OData.publish: true` annotation is STILL NOT VISIBLE in the source!
- Source code shows same annotations as before (missing line 5 with OData.publish)

### Current Investigation Status

The abapGit pull shows the correct commit hash (873c7e5) but the actual CDS source code doesn't reflect the changes. Possible causes:

1. The pull may have marked objects as "diff" but not actually replaced the source
2. The inactive version may have the changes (need to check Aktiv <-> Inaktiv)
3. There may be a conflict that wasn't properly resolved

### Next Steps Required

1. Check the inactive version of the CDS view (Aktiv <-> Inaktiv toggle)
2. If annotation exists in inactive version, activate the CDS view
3. If annotation doesn't exist, investigate why the pull didn't update the source
4. After annotation is confirmed, retry OData service registration

### Files Modified in Git

- `C:\github\hfqbo4e\src\#hfq#cds_bo_vertrag.ddls.asddls` - Added `@OData.publish: true`
- `C:\github\hfqbo4e\src\#hfq#cds_bo_malo_202106.ddls.asddls` - Added TODO placeholder

### Technical Details

- SAP System: S4U (100)
- Client: 100
- User: KLEINK
- Package: /HFQ/BO4E
- Transport: S4UK901098 (locked by MANNN)
