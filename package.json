{
  "name": "sapwebguimcp",
  "private": true,
  "description": "JavaScript formatting for SAP Web GUI MCP Server",
  "devDependencies": {
    "prettier": "3.7.4"
  },
  "scripts": {
    "format": "prettier --write src/**/*.js",
    "format:check": "prettier --check src/**/*.js"
  }
}
